const responseData = pm.response.json();

pm.test("Status code is 422", function () {
    pm.response.to.have.status(422);
});

pm.test("Response contains valid error message ", function () {
    pm.expect(responseData.errors.email[0]).to.equal("The email has already been taken.");
});
pm.test("Status code is 422 ", function () {
    pm.expect(responseData.status_code).to.equal(422);
});
pm.test("Status is false ", function () {
    pm.expect(responseData.status).to.equal(false);
});
